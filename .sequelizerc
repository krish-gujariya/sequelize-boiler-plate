'use strict';

const path = require('path');

module.exports = {
  config: path.resolve(path.join(process.cwd(), 'src/sequelizeDir'), 'config.js'),
  'seeders-path': path.resolve(path.join(process.cwd(), 'src/sequelizeDir'), 'seeders'),
  'migrations-path': path.resolve(path.join(process.cwd(), 'src/sequelizeDir'), 'migrations'),
};
